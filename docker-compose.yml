version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
